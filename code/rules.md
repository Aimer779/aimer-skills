锁死项目文件结构    utils    pages   components

锁死技术栈版本

锁死设计系统

路径别名

@/ → 项目源码根目录（src/）
@components/ → 组件库
@hooks/ → 自定义 Hooks
@utils/ → 工具函数
@types/ → 类型定义
@assets/ → 静态资源

打包运行 ,验证前后端通信链路

注意交互式命令会阻塞Agent,自己手动去运行,遇到不会的,不清楚的去问AI
